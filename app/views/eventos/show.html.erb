<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Visualizacion Eventos</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f4f8;
            margin: 0;
            padding: 0;
        }

        header {
            background-color: #3a6ea5;
            padding: 20px;
            text-align: center;
            color: white;
        }

        .container{
          max-width: max-content;
          max-height: max-content;
          margin: 20px auto;
        }

        .Evento,
        .Reserva {
            text-align: center;
            background-color: white;
            margin: 20px auto;
            border-radius: 50px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            padding: 50px;
            height: 100%;
            width: 100%;
        }

    </style>
</head>
<body>
    <header>
        <%= "This is the event : #{@evento.eventName}" %>
    </header>
    <div class="container">
        <div class="Evento">
            <h1><%= @evento.eventName %></h1>
            <h2><%= @evento.description %></h2>
            <p>The day is: <%= @evento.dia.strftime("%d-%B-%Y") %></p>
            <p>From: <%= @evento.start_time.strftime("%I:%M %p") %></p>
            <p>To: <%= @evento.end_time.strftime("%I:%M %p") %></p>
            <p>The duration of the meetings are: <%= @evento.plazoDeTiempo.strftime("%H hs ; %M min")%></p>
            <h3>This is the link others can use to book: <%= link_to reservas_path,:reservas, class: "index-link" %></h3>
        </div>
        <div class="Reserva">
            <% if @reservas.size == 0 %>
                <h2>No one booked for this event</h2>
            <% else %>
                <% @reservas.each do |reserva|%>
                    <div style="color: red">
                        <ul>
                            <li>
                                <h2>You have a reservation of: <%= reserva.email %></h2>
                                <h2>Starting at:<%= reserva.horario.strftime("%I-%M %p") %></h2>
                            </li>
                        </ul>
                    </div>

                <% end %>
            <% end %>
        </div>
    </div>
</body>
</html>
